# Generation Card

兼容入口：通用成稿先使用 `references/structured-input-template.md`，再把声音、事实、场景链和事件调用边界编译成当前任务的 `WRITING_INPUT`。本文件不包含特定模型、路由、项目路径或字数硬门。

最小字段：任务目标、读者、体裁、事实来源、视角、人物目标、场景顺序、未知项、禁止推断、声音参数、事件调用范围、输出格式和回归检查。
